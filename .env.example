# Obrigatória — o backend é um serviço remoto (ex.: Railway), nunca localhost.
# Sem esta variável definida, a aplicação recusa fazer pedidos (ver src/lib/api.ts).
VITE_API_BASE_URL=https://eletro-vendus-production.up.railway.app
